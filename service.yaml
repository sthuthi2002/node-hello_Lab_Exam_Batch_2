apiVersion: v1
kind: Service
metadata:
  labels:
    app: monitorappsec
  name: monitorappsec
spec:
  type: NodePort
  ports:
    - port: 3000
      targetPort: 3000
      protocol: TCP
  selector:
    app: monitorappsec
